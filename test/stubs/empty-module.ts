// Stub for build-time-only imports (e.g. `server-only`) that have no runtime
// implementation outside the Next.js bundler. Aliased in `vitest.config.ts` so
// the modules under test can be loaded in plain Node by Vitest.
export {};
